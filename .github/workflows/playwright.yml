name: Playwright Smoke Tests Only

on:
  push:
    branches: [ main, develop, ScalableVersion ]
  workflow_dispatch:

jobs:
  smoke-tests:
    timeout-minutes: 30
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Clean previous Playwright report
        run: rm -rf playwright-report

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run smoke tests
        env:
          HEADLESS: 'true'
        run: npx playwright test --grep @smoke

      - name: Upload Playwright HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report/
          retention-days: 30

      - name: Upload Playwright Failure JSON
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-failures-json
          path: reports/failures.json
          retention-days: 30